/** True when the company name has multiple words (e.g. "Multi Play"). */
export declare function isMultiWordCompanyName(name: string): boolean;
/**
 * Text used in lettermark prompts:
 * - one word → full word ("Modo")
 * - multiple words → initials ("Multi Play" → "MP")
 */
export declare function lettermarkTextFromName(name: string): string;
//# sourceMappingURL=lettermark-text.d.ts.map